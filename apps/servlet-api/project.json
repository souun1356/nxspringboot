{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "servlet-api",
  "sourceRoot": "apps/servlet-api/src",
  "projectType": "application",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["mvnw jetty:run"],
        "cwd": "apps/servlet-api"
      }
    }
  },
  "tags": []
}
